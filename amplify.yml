version: 0.1
frontend:
  phases:
    preBuild:
      commands:
        - npm install
    build:
      commands: 
        - npm run-script build
  artifacts:
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
